version: "3"

services:
  n8n:
    image: n8nio/n8n
    container_name: n8n
    ports:
      - "5678:5678"
    restart: always
    environment:
      - N8N_HOST=0.0.0.0
      - N8N_SECURE_COOKIE=false
